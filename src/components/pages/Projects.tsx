// Lib
import React from 'react';
import { Link } from 'react-router-dom';

const Projects = () => {
  return (
    <div>
      <Link to="wasm-game-of-life">Conway&apos;s Game of Life in Rust + WASM</Link>
    </div>
  );
};

export default Projects;
